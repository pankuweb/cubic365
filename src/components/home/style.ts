import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
  },
  card: {
    width: '31%',
    height: 110,
    backgroundColor: 'red',
    color: '#fff',
    borderRadius: 5,
    justifyContent: 'center',
    padding: 10,
  },
});
